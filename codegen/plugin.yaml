tasks:
  generate:
    action: !io.github.elektrofuzzis.ftswarm_clients.codegen.generateKotlinSources
      apiDefPath: ${project.rootDir}/api
      generatedSourceDir: ${taskOutputDir}

generated:
  sources:
    - language: kotlin
      directory: ${tasks.generate.action.generatedSourceDir}