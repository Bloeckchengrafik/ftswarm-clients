name: reedSwitch
extends: value-boolean
init:
  - !<set_io_type>
    type: SWOSIO_REEDSWITCH
    parameters:
      - name: normallyOpen
        type: boolean
        defaultValue: false
  - !<subscribe>
