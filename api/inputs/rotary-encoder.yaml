name: rotaryEncoder
extends: value-int
init:
  - !<set_io_type>
    type: SWOSIO_ROTARYENCODER
    parameters:
      - name: normallyOpen
        type: boolean
        defaultValue: false
  - !<subscribe>
