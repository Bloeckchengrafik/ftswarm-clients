name: frequencyMeter
extends: value-int
init:
  - !<set_io_type>
    type: SWOSIO_FREQUENCYMETER
    parameters:
      - name: normallyOpen
        type: boolean
        defaultValue: false
  - !<subscribe>
