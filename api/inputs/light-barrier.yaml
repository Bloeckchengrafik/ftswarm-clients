name: lightBarrier
extends: value-boolean
init:
  - !<set_io_type>
    type: SWOSIO_LIGHTBARRIER
    parameters:
      - name: normallyOpen
        type: boolean
        defaultValue: false
  - !<subscribe>
