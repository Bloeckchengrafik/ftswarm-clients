name: voltmeter
extends: value-int
init:
  - !<set_io_type>
    type: SWOSIO_VOLTMETER
    parameters:
      - name: normallyOpen
        type: boolean
        defaultValue: false
  - !<subscribe>
members:
  getVoltage:
    command: getVoltage
    parameters: []
    returnType: float
